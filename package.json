{
  "name": "portfolio-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Canonical, website-agnostic source of truth for Diogo's projects. Generated from GitHub repos (topic: portfolio) + hand-authored overrides.",
  "engines": { "node": ">=18" },
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "candidates": "node scripts/sync.mjs --candidates",
    "check": "node scripts/sync.mjs --check",
    "build:v1": "node adapters/to-v1-vanilla.mjs",
    "build:readme": "node adapters/to-profile-readme.mjs"
  }
}
